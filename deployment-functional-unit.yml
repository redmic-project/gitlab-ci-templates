# Only for backward compatibility, migrate ASAP your project includes to templates included here!

include:
  - local: '/deployment-service/functional-unit/docker-deploy.yml'
  - local: '/deprecation-warning.yml'
